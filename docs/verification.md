# 验证记录

日期：2026-09-22。Windows x64；MSVC 19.51.36244；CMake 4.2；librime 1.17.0。

另外在 GitHub Actions Windows Server 2022 / VS2022 / v143 环境完成全量验证：[成功的构建记录](https://github.com/hetaodw/TypePick/actions/runs/35711722968)。该记录对应代码提交 `0133526104fc552de71bb0b9b6e6b2d3b196ff59`。此后增加了下述本地真实 API 诊断工具，并重新通过本地编译、原生测试和五项 Rime 联调；服务端推荐逻辑未改动。

| 项目 | 结果 |
| --- | --- |
| 原生模块、WinHTTP 客户端、推荐窗口、TypePickProbe 编译链接 | 通过 |
| 候选/配置/答案校验、默认关闭、防抖、跨会话旧结果丢弃、撤销、不阻塞提交、超时、越界、异常回退 | 原生测试通过 |
| Rime 输入 `yanjiu`，上下文“这个问题需要进一步”，演示推荐后 Tab | 实际上屏“研究” |
| Rime 输入 `yanjiu`，上下文“这家商店主要卖”，演示推荐后 Tab | 实际上屏“烟酒” |
| 已显示推荐后失焦、原始输入变化、应用变为 chrome.exe | 三种场景均拒绝旧推荐，没有提交文本 |
| Tab 采用后的松开事件 | 被消耗，不泄漏到应用 |
| 小狼毫固定版本补丁应用、重复准备源码 | 通过 |
| Jev 远端认证、原生 WinHTTP、真实 Rime 选词 | 已用用户提供的本地密钥验证两个场景；整体中文推荐质量尚未评测 |
| 完整 WeaselServer / TSF 构建 | 云端通过，实际链接生成 `WeaselServer.exe` 和 `weaselx64.dll` |
| 注册系统输入法、真实记事本/浏览器/Office/游戏兼容性 | 未执行 |

自动化五项 Rime 桥接测试调用的是真实 `rime.dll`；推荐来源为本地 demo 固定规则。新增的 Jev 实测直接调用原生 WinHTTP 客户端，并按实际响应让 Rime 上屏，但尚未覆盖真实模型返回后的窗口/Tab 流程。这些测试不代表已经验证实际 TSF 安装、应用端 IPC 流程或整体模型效果。完整构建在云端完成，本机仍缺少 ATL；此次 CI 只保留测试记录，没有发布安装包或保存服务端二进制制品。

运行 `scripts/Test-Rime.ps1` 会重新生成独立的 `build/probes/<编号>/results.json`，包含每次测试的输入、上下文、commit 或 rejected 字段。`scripts/Build-Core.ps1` 执行原生测试。不要把本记录理解为正式发布验收。

## Jev 真实调用（2026-09-22）

使用 `.env` 的单行 `key="..."`，临时映射到测试进程环境变量；密钥不在结果、文档和 Git 中。默认 600 ms 下，两次原生请求均返回 `timeout`，没有提交文字。一个独立 HTTP 诊断请求成功，耗时 678 ms，选中“烟酒”但置信度仅 0.38，按当前规则应拒绝采用。

随后使用 `Test-Jev.ps1`，诊断时限 3000 ms，保持 0.7 置信度和 0.1 概率差门槛，得到：

| 拼音 / 上下文 | 实际 Rime 上屏 | Jev 置信度 | 原生请求及轮询耗时 |
| --- | --- | --- | --- |
| yanjiu / 这个问题需要进一步 | 研究 | 0.99 | 714 ms |
| yanjiu / 这家商店主要卖 | 烟酒 | 0.91 | 809 ms |

本地证据：`build/live-probes/1031bdf7b507460ebf45e98a47e96dbf/results.json`。这些是单次观测；独立诊断与原生请求的 JSON 字段顺序不同，不能把置信度差异全部归因于模型随机性。没有因此降低采用门槛或修改默认输入法时限，后续仍需统一请求数据集进行质量和延迟评测。
